---
# Host-specific variables for debian-vm
# This is an EXAMPLE file - actual values should be provided via command line or environment
#
# To use external configuration files:
# make deploy VM_HOST=your.ip TARGET_USER=username ENV_FILE=/path/to/.env MCP_FILE=/path/to/mcp-servers.json

# Example override paths (update these to match your setup)
# env_config_path: "/path/to/your/.env"
# mcp_servers_config_path: "/path/to/your/mcp-servers.json"

# The Makefile and playbooks handle dynamic path resolution
# No hardcoded paths should be committed to this file