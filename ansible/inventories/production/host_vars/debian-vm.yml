---
# Host-specific variables for debian-vm (192.168.1.113)
# References existing configs from ~/.config/claude-code-mcps

# Override config paths to use existing files
env_config_path: "{{ ansible_env.HOME }}/.config/claude-code-mcps/.env"
mcp_servers_config_path: "{{ ansible_env.HOME }}/.config/claude-code-mcps/mcp-servers.json"

# Ensure we use the control machine's configs
local_env_config_path: "~/.config/claude-code-mcps/.env"
local_mcp_servers_config_path: "~/.config/claude-code-mcps/mcp-servers.json"